<header>
    <nav class="navbar navbar-expand-md navbar-light fixed-top bg-primary">
        <a class="navbar-brand" href="{{ path('site_home') }}" title="Home">
            <img src="https://quizclass.s3-sa-east-1.amazonaws.com/assets/images/logo-quiizclass.svg" alt="Logo Quiz Class" height="30">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('site_solucoes') }}">{% trans %}menu.Soluções{% endtrans %}</a>
                </li>
                {#<li class="nav-item">
                    <a class="nav-link" href="#">{% trans %}menu.Exemplos{% endtrans %}</a>
                </li>#}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('site_planos') }}">Preços</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link border-radius border-dark text-center" href="{{ path('site_criar_quiz_ajuda', {'utm_source': 'direct'}) }}" style="width: 118px;"><i class="fas fa-plus"></i>Criar quiz</a>
                </li>
               {# <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-play-circle"></i>Criar sala de aula</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fas fa-sign-in-alt"></i><u>Entrar</u></a>
                </li>#}
                <li class="nav-item">
                    {% if  app.user and app.user.tipo == 'ALUNO'%}
                        <a class="nav-link" href="{{ path('aluno_dashboard')}}">{{ app.user.nome }}</a>
                    {% elseif  app.user and app.user.tipo == 'ADM_QUIZ'%}
                        <a class="nav-link" href="{{ path('admin_dashboard')}}">{{ app.user.nome }}</a>
                    {% elseif app.user %}
                        <a class="nav-link" href="{{ path('app_dashboard')}}">{{ app.user.nome }}</a>
                    {% else %}
                        <div class="dropdown">
                            <a class="nav-link" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fas fa-walking"></i><u>Entrar</u></a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                                <a 
                                    class="dropdown-item" 
                                    href=
                                        {% if app.user and app.user.tipo == 'ALUNO' %}
                                            "{{ path('aluno_dashboard') }}"
                                        {% else %}
                                            "/login/aluno"
                                        {% endif %}
                                >
                                    Área do Aluno
                                </a>
                                <a class="dropdown-item" href="/login/professor">Professor/Administrador</a>
                            </div>
                        </div>
                    {% endif %}
                </li>

                {# <li class="nav-item form-lead">
                    <form class="form-inline">
                        <input type="email" class="form-control border-radius-left border-dark" placeholder="Seu e-mail aqui">
                        <button class="btn btn-dark border-radius-right btn-xs-block" data-event-category="User"
                                data-event-action="create-free-account" style="line-height: 1.6;">TESTE GRÁTIS</button>
                    </form>
                </li> #}
            </ul>


        </div>
    </nav>
</header>