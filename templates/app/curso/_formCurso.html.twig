{{ form_start(form) }}
    <div class="row">
        <div class="col-12 col-md-6 col-lg-6 col-xl-6">
            {{ form_row(form.nome) }}
        </div>

    </div>
    {{ form_row(form.salvar) }}
{{ form_end(form) }}