{{ form_start(form) }}
    <div class="row">
        <div class="col-sm-8">
            {{ form_row(form.nome) }}
        </div>
        
    </div>
    <br/>
    <div class="row">
        <div class="col-md">
            {{ form_row(form.telefone) }}
        </div>
        <div class="col-md">
            {{ form_row(form.email) }}
        </div>
        <div class="col-md">
            {{ form_row(form.senha) }}
        </div>
    </div>
    <br/>
    
    {{ form_row(form.status) }}

    </br>
    <button class="btn shadow-default btn-default d-flex align-items-center" type="submit">Finalizar matrícula</button>
{{ form_end(form) }}     