<!-- Modal -->
<div class="modal fade" id="modalDelete{{quiz.quiz.id}}" tabindex="-1" role="dialog" aria-labelledby="modalDelete{{quiz.quiz.id}}Title" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Deletar Quiz?</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body d-flex flex-row justify-content-center">
                <button class="btn btn-default btn-secondary shadow-default mr-5" data-dismiss="modal">
                    Cancelar
                </button>
                <a class="btn btn-default btn-danger shadow-default flex-center-default" href="{{ path('app_delete_quiz', {'quiz_id': quiz.quiz.id}) }}" title="Deletar" style="width: 75px; margin: 0;">
                    Deletar
                </a>
            </div>
        </div>
    </div>
</div>